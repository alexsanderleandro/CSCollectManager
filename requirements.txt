# CSCollectManager - Dependências

# Interface Gráfica
PySide6>=6.6.0

# Banco de Dados
SQLAlchemy>=2.0.0
pyodbc>=5.0.0

# Processamento de Imagens
Pillow>=10.0.0

# HTTP (envio de cargas para a API CSCollect)
requests>=2.31.0

# Utilitários (zipfile é builtin, não precisa instalar)

# Criptografia (assinatura de arquivos)
# Usamos HMAC-SHA256 (builtin) — cryptography não é mais necessária

# Desenvolvimento (opcional)
# black>=23.0.0
# pylint>=3.0.0
# pytest>=7.0.0
